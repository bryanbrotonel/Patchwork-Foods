export interface TypeCartItem {
  id: number;
  name: string;
  price: number;
  quantity: number;
}

export interface TypeShopItem {
  id: number;
  name: string;
  price: number;
  image: string;
  description: string;
}